<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jacee.magicdialog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MagicDialog">
        <activity android:name=".EditActivity"></activity>
        <activity
            android:name=".TestJavaActivity"
            android:exported="false" />
        <activity
            android:name=".CustomThemeTwoActivity"
            android:exported="false"
            android:theme="@style/Theme.MagicDialog.Custom2" />
        <activity android:name=".OptionsActivity" />
        <activity
            android:name=".CustomThemeActivity"
            android:label="Custom"
            android:theme="@style/Theme.MagicDialog.Custom" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>